#include <stdio.h>
#include "app.h"

/* Implementación mínima para que no sea una unidad vacía.
   Más adelante completamos el menú real. */
void app_run(void) {
    puts("======================================");
    puts(" Reproductor & Recomendador Offline ");
    puts("   (AED2 - MVP, modulo app.c listo)  ");
    puts("======================================");
}
